[US-004] demo software — DONE
- hello.s (character-mapped banner via font8x8), snake.s (playfield + food), cube.s
  (rotating 3D wireframe cube, 6502 asm)
- three real cube bugs found via instruction-level probe + per-edge reference diff:
  (1) shared DXA tail clobber, (2) `bpl` over `sta ERR` discarded the Bresenham
  accumulator every iteration -> x-dominant lines with dy>0 drew flat; branch on
  carry and store unconditionally, (3) dropped low-add carry in TBL 16-bit pointer
  -> rotations >=15 indexed 256 bytes early
- tests/draw_probe.rs: 5 rotations x 12 edges pixel-exact vs independent reference
- frame timing: first draw pass runs during frame 5 (frame-tick gate); demos use 6 frames
- lessons: byte-level ROM patching unreliable (equates assemble absolute) — patch
  at the SOURCE level; decode addresses from the assembled ROM dynamically

[US-005] wasm hardware console — DONE
- src/wasm.rs: hand-rolled C-ABI over a global Machine+Cpu (no wasm-bindgen);
  12.7 KB cdylib with 10 vin_* exports
- console/: Vite + vanilla TS. .vin format is INLINE per-segment payload
  (addr,len,bytes...) — not a flat table (parser bug #1 caught in browser);
  heap.set at __heap_base can exceed the default 2-page memory -> grow first
  (bug #2). wasm32 release build + npm run build both verified.
- browser-verified via playwright: CRT screen renders snake + cube demos,
  keyboard arrows move the snake, CPU panel shows live registers
- lesson: vite preview serves public/ with caching; hard-reload when
  re-emitting ROMs

## US-006 complete (2026-08-29)
- Full suite green: 76 passed / 0 failed incl. Klaus functional-test gate
- README.md: memory map, I/O registers, ISA coverage, build/run, verification section
- docs/: 6 console screenshots (hello/snake/cube) captured via playwright
- vintage-artifact.html: self-contained playable console (wasm + 3 ROMs base64-inlined), live-verified
- Lesson: claude.ai artifact publishing needs a claude.ai login; sessions on ANTHROPIC_AUTH_TOKEN
  cannot publish. Delivered as a standalone file; verify it via playwright before handing over.

## Post-review fixes (2026-08-29)
- snake: crash now lands ON the wall cell (not past wrap), food respawns
  interior-only, +/- (0x15/0x16) retune the move divider 1..8 — locked by
  tests/snake_probe.rs (wall never touched x=31; speed keys change step rate)
- hello: root-caused the "all text crammed at the top" bug — 8-pixel text
  rows are 256 framebuffer bytes apart, not 32; banner rebuilt (title, rule,
  subtitle, READY. + blinking cursor), locked by tests/hello_probe.rs
- deslop: Default impl for Cpu, clippy-clean across all targets
- post-deslop regression: 79 passed / 0 failed, clippy clean
- README test count bumped to 76 pre-deslop, actually 79 now — next pass
  should keep the number generated from a real run
